---
// ponytail: locale mirror of ../services.astro
import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/ui/Hero.astro';
import Features from '../../components/ui/Features.astro';
import Steps from '../../components/ui/Steps.astro';
import FAQs from '../../components/ui/FAQs.astro';
import CTA from '../../components/ui/CTA.astro';
import { t } from '../../lib/i18n';

const content = t(Astro.currentLocale);
const { seo, hero, features, steps, faqs, cta } = content.services;
---

<BaseLayout title={seo.title} description={seo.description}>
  <Hero title={hero.title} subtitle={hero.subtitle} />
  <Features title={features.title} features={features.items} />
  <Steps title={steps.title} steps={steps.items} />
  <FAQs title={faqs.title} faqs={faqs.items} />
  <CTA title={cta.title} description={cta.description} action={cta.action} />
</BaseLayout>
